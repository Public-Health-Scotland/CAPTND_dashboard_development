


how_to_use_plot_text <- tagList(
  tags$h4("How to use this plot?"),
  tags$ul(
    tags$li("Move the cursor over the data points to see the data values"),
    tags$li("Single click on legend variables to remove the corresponding trace"),
    tags$li("Double click on legend variables to isolate the corresponding trace"),
    tags$li("Double click on the legend to restore all traces"),
    tags$li("Click the camera icon in the top right to download the plot as a PNG file")
  )
)




